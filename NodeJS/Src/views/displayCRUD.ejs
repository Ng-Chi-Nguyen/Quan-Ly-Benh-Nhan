<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Display CRUD</title>

   <!-- Latest compiled and minified CSS -->
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">

   <!-- jQuery library -->
   <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js"></script>

   <!-- Popper JS -->
   <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>

   <!-- Latest compiled JavaScript -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>

</head>
<style>
   a {
      text-decoration: none;
   }

   .p-5px {
      padding: 5px !important;

   }
</style>

<body>
   <div class="container">
      <table class="table table-striped">
         <thead>
            <tr class="text-center">
               <th scope="col">ID</th>
               <th scope="col">Email</th>
               <th scope="col">First Name</th>
               <th scope="col">Last Name</th>
               <th scope="col">Address</th>
               <th scope="col">Phone</th>
               <th scope="col">Gender</th>
               <th scope="col">Action</th>
            </tr>
         </thead>
         <tbody>
            <% for(let i=0; i<dataTable.length; i++){ %>
               <tr>
                  <td class="text-center">
                     <%= dataTable[i].id %>
                  </td>
                  <td>
                     <%= dataTable[i].email %>
                  </td>
                  <td class="text-center">
                     <%= dataTable[i].firstName %>
                  </td>
                  <td class="text-center">
                     <%= dataTable[i].lastName %>
                  </td>
                  <td class="text-center">
                     <%= dataTable[i].address %>
                  </td>
                  <td class="text-center">
                     <%= dataTable[i].phoneNumber %>
                  </td>
                  <td class="text-center">
                     <% if(dataTable[i].gender===1){ %>
                        Female
                        <% } else { %>
                           Male
                           <% } %>
                  </td>
                  <td class="p-5px text-center">
                     <a href="/delete-crud?id=<%= dataTable[i].id %>" class="btn btn-danger">
                        Delete
                     </a>
                     <a href="/edit-crud?id=<%= dataTable[i].id %>" class="btn btn-success">
                        Edit
                     </a>
                  </td>
               </tr>
               <% } %>
         </tbody>
      </table>
   </div>
</body>

</html>